import { Typography } from "../Typography";

export const RollableTable = () => {
    return (
        <div>
            <Typography className="text-slate-900">Rollable Table</Typography>
        </div>
    );
};
