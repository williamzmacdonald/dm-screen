import { Typography } from "../Typography";

export const NPCGenerator = () => {
    return (
        <div>
            <Typography className="text-slate-900">NPC Generator</Typography>
        </div>
    );
};
